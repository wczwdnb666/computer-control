---
name: "computer-control"
description: "让我能看见你的屏幕、并且像人一样动鼠标键盘：截图、点击、右键、拖拽、输入中英文、读写剪贴板、找窗口、等界面加载。纯 ctypes 调 user32，不用装 pyautogui 那一堆东西。适合操作那些没有 API 的图形界面程序——填表单、点按钮、在两个软件之间搬数据、把重复的桌面操作自动化。"
version: "2.0.0"
triggers:
  - "鼠标"
  - "键盘"
  - "点击"
  - "右键"
  - "截图看屏幕"
  - "操控桌面"
  - "GUI 自动化"
  - "computer use"
  - "computer-control"
---

# 桌面操控

这是我用来"上手"的工具——字面意义上的上手。有了它我能看见你的屏幕，能移动鼠标、点按钮、打字。

写在前面：**这东西真的能操作你的电脑，用错会改坏东西。** 我把踩过的坑都写在下面的"血泪教训"里了，用之前请务必看完那一节。

## 怎么跑

```
$PY = C:\Users\到达胜利之前 无法回头\.workbuddy-ai\binaries\python\versions\3.13.12\python.exe
$CC = C:\Users\到达胜利之前 无法回头\.workbuddy-ai\skills\computer-control\scripts\cc.py

$PY $CC <命令> [参数]
```

**别用裸 `python`**——这台机器上 PATH 里的 `python` 不一定指向托管解释器，会直接报模块找不到。

## 先看，再动

任何操作之前，第一件事永远是截图。不看就点，等于闭着眼睛开车。

```bash
$PY $CC shot --out C:\tmp\s.png --grid 300 --maxw 1400
```

然后用 Read 工具看这张图。三个参数值得说一下：

- `--grid 300` 会在图上叠一层坐标网格，**网格边上标的是真实像素值**。为什么要有这个？因为我最早没做，对着截图目测坐标，点偏了三次才发现是自己换算错了。
- `--maxw 1400` 把 2880 宽的图缩到 1400，不然 Read 看的时候会糊。但注意——**缩放之后图里的坐标不是真实坐标了**，得按 `真实 = 图中坐标 × (2880 ÷ 图中宽度)` 换算。有网格就不用算，直接读。
- `--win` 或 `--process` 可以只截某个窗口，比全屏清爽得多。

只想看某个点是什么颜色的话，用 `pixel --x X --y Y`，比截一整张图便宜。

## 动鼠标

```bash
$PY $CC move 800 600                 # 移过去
$PY $CC hover --x 800 --y 600        # 移过去停着，不点
$PY $CC click --x 800 --y 600        # 左键单击
$PY $CC rclick --x 800 --y 600       # 右键单击
$PY $CC dclick --x 800 --y 600       # 左键双击
$PY $CC mclick --x 800 --y 600       # 中键单击
$PY $CC drag 100 100 500 500         # 按住拖过去
$PY $CC scroll -3                    # 滚轮，负数是往下
```

`down` 和 `up` 是分开按住和松开，配合 `move` 能做长按拖动：

```bash
$PY $CC move 300 300
$PY $CC down
$PY $CC move 900 700
$PY $CC up
```

### 坐标可以用"窗口内相对位置"

这是最省事的一个设计。你直接给屏幕绝对坐标也行，但**窗口一被拖动，写死的坐标就全废了**。所以我让所有点击命令都能接受窗口相对坐标：

```bash
$PY $CC click --win 记事本 --rx 400 --ry 60    # 客户区左上角往右400、往下60
$PY $CC click --process chrome --center        # 直接点窗口正中间
```

窗口怎么找，下面单独说。

### 鼠标轨迹是"不完美"的

默认情况下鼠标不是"从 A 匀速滑到 B"。它按真人的运动模型走，包含四个特征：

**1. 反应延迟。** 决定移动之后，手不会立刻动——先停 45~160 毫秒。

**2. 多段子运动。** 这是最关键的一条。真人搬鼠标不是一次到位，而是：

```
弹道冲刺  →  停一下  →  修正  →  停一下  →  微调
（大部分距离）        （小段距离）      （最后几个像素）
```

每段覆盖"剩余距离"的固定比例（约 86%），所以后段距离越来越小、但**时间占比不按比例缩**——速度自然降下来。如果距离和时间一起缩，全程匀速，一眼就假。

**3. 非对称速度曲线。** 每段内部是加速快、减速慢，不是对称的缓动。

**4. 低频手抖。** 生理性手抖是 8~12Hz，不是每个采样点换个随机值。如果按步更新，抖动频率会到 200Hz——看起来像鼠标坏了，不像人手。所以这里每 16~26 个采样点才取一个新随机值，中间插值。

**实测对比**（1664 像素的距离）：

| 模式 | 速度峰数 | 速度剖面 | 偏离直线 | 落点误差 |
|---|---|---|---|---|
| `--no-human` | **2 个** | 对称钟形（加速→匀速→减速）——机器人的指纹 | 0.8 px | 0.0 px |
| 默认 | **5 个** | 冲刺→回落→修正→回落→微调，尾部缓慢收尾 | 12 px | **0.0 px** |

实测路程 1672px vs 直线 1664px——**只多走 0.5%，像人但没在乱晃**。

移动耗时也是按距离自动估算的（`--duration -1`，默认），公式大致是 `170 + 距离 × 0.42` 毫秒，钳制在 200~1150ms。真人对不同距离的手感不一样，远距离不会一秒就到。

需要绝对直线（比如对着绘图参考线对齐）时加 `--no-human`；需要瞬间到位加 `--duration 0`。

## 打字

```bash
$PY $CC type "你好 world"            # 中文英文都行
$PY $CC key enter                    # 按一个键
$PY $CC hotkey ctrl shift s          # 组合键
```

中文能打是因为走的是 `KEYEVENTF_UNICODE` 而不是虚拟键码——虚拟键码根本表达不了汉字。

速度上，输入是**攒成一个批次一次性提交**的。实测 36 个中英混合字符 **0.004 秒**。逐字符调用 `SendInput` 会慢一个数量级，没必要。

## 剪贴板

```bash
$PY $CC clip --set "要粘贴的内容"
$PY $CC clip --get
```

别小看这个。读回剪贴板是**验证输入是否成功**最可靠的办法——比看截图准，因为不用我肉眼判断。示例脚本里就用了这招做二次校验。

## 找窗口

```bash
$PY $CC windows                      # 列出所有可见窗口
$PY $CC windows --process chrome     # 只看 Chrome 的
$PY $CC info --win 记事本            # 看它的位置、尺寸、类名
$PY $CC focus --process bilibili     # 激活它
$PY $CC waitwin --process bilibili --timeout 10   # 等它出现
$PY $CC winstate --win 记事本 --action max        # 最大化/最小化/还原/关闭
```

三种定位方式，**请优先用 `--process`**：

```
--win      "标题里的一段文字"      脆弱，Electron 应用的标题一直在变
--process  bilibili               稳，进程名不会变
--class    Chrome_WidgetWin_1     最精确
```

为什么强调这个？因为我吃过亏。当时我按「Notepad」这个通用词去匹配窗口，结果没匹配到我刚启动的那个，脚本回退激活了你**已经打开的另一个记事本**——27 个测试字符打进了你正在编辑的 `server.log`。幸好我在保存提示上按的是"不保存"，没造成损失。

**所以：匹配窗口时永远用唯一字符串，别用通用词。**

## 等界面反应过来

```bash
$PY $CC stable --timeout 10          # 阻塞到屏幕不再变化
$PY $CC waitwin --process X --timeout 15   # 阻塞到窗口出现
```

两个都会在超时后以退出码 3 结束，脚本里可以据此判断。

`stable` 是等页面加载完最好用的办法——比 `sleep 3` 靠谱，因为快的时候它 0.7 秒就返回了，慢的时候它会一直等。

## 标准流程

**照这个顺序做，一步都别省：**

```
1. shot --grid 300 --maxw 1400     → 先看，找到目标在哪
2. focus --process <应用>           → 激活它，核对输出的标题对不对
3. --dry-run click ...             → 干跑一遍，确认参数没错
4. click ...
5. stable --timeout 8              → 等界面反应完
6. shot --out after.png            → 截图确认真的生效了
```

第 2 步和第 6 步是最容易被跳过的，也是最重要的两步。**点了不看结果的自动化是负债，不是资产。**

`--dry-run` 会打印"将要做什么"而不执行，任何不确定的操作都该先干跑一遍。

## 坐标系统（这个搞错了全盘皆输）

- `screen` 返回的是**物理分辨率**，这台机器是 **2880×1800**。
- 截图默认也是 2880×1800，**和鼠标坐标系完全一致**，图上的像素坐标可以直接用。
- 用 `--maxw` 缩放后就不一致了，要按比例换算回去。
- 多显示器的话用 `monitors` 拿每块屏的边界，注意虚拟桌面坐标**可能是负的**。

我在这上面栽过一次：程序里忘了调 `SetProcessDpiAwareness`，结果 `GetSystemMetrics` 返回的是逻辑分辨率 1440×900，而截图是物理分辨率 2880×1800。**所有坐标差了一倍**，点哪儿哪儿不对。这个坑现在在脚本里已经填了，但你自己写代码调这些 API 时也要记得。

## 血泪教训

这几条是真金白银换来的，请认真看：

**1. 打字前必须确认目标窗口——用 `--require` 硬闸，不要靠自觉。**

`type` 是打到"当前有焦点的窗口"的。我知道这条规矩，还是犯了一次：脚本里打印了前台窗口却**没做判断**就打字，结果把一串 URL 打进了 WorkBuddy 的输入框。

所以现在 `type` 支持 `--require`，前台进程名不匹配直接拒绝执行（退出码 5）：

```bash
$PY $CC type "内容" --require msedge    # 前台不是 Edge 就拒绝
```

**写自动化脚本时，每次打字都加上它。**

**2. 长文本用 `--paste`，不要逐字符模拟按键。**

批量 `SendInput` 对浏览器和 Electron 应用**太快了，会静默丢字符**。实测往 GitHub 的描述框里打 132 个字符，只进去 130 个，而且拼写检查把丢字后的 `screensho` 标了红。

```bash
$PY $CC type "长文本…" --paste --require msedge
```

`--paste` 走剪贴板（写剪贴板 → Ctrl+V），是原子的，多长都不丢。默认会**恢复你原来的剪贴板内容**；不需要恢复就加 `--no-restore`。

**3. 滚动之后，所有坐标作废。**

页面/窗口一旦滚动，之前量好的坐标全部失效。**要么先滚到目标位置再测量，要么滚完重新截图测量。** 我在这上面连栽两次：一次是滚动后点击落到了别的元素上，一次是误触发了页面的搜索浮层。

**4. 点击前先截图确认坐标，点完再截图确认结果。**

盲点必错。正确姿势是：截图 → 读坐标 → 点击 → 再截图验证。宁可多花两张截图，也别赌。

**5. 绝对不要用 `hotkey alt f4` 关窗口。** 会弹保存确认框，后面排队的按键很容易误触"保存"。关自己启动的进程请直接结束进程（Python 里 `Popen.terminate()`）。

**6. 破坏性操作前先 `--dry-run`。**

**7. 删除、发送、支付、提交表单这类动作，必须先跟用户说清楚并等他确认。** 这不是建议，是硬规矩。

**8. 自己启动的测试程序，自己关掉。** 别留在用户桌面上。

## 改脚本时要注意的

如果你要动 `cc.py`，这几个点错了会**静默失败**（不报错，但也没效果）：

- `INPUT` 结构体在 x64 下**必须正好 40 字节**，字段顺序和类型都不能错。
- 中文必须走 `KEYEVENTF_UNICODE`，虚拟键码打不出来。
- 必须调 `SetProcessDpiAwareness(2)`，否则坐标全部错位。
- `SetForegroundWindow` 在前台锁被占用时会失败，需要先用 `AttachThreadInput` 绕过去（看 `force_foreground` 函数）。
- 批量提交按键：把 N 个 KEYDOWN/KEYUP 攒成一个数组，一次 `SendInput`。

## 依赖

- 截图和 `stable` 需要 **Pillow**（这台机器上的托管 Python 已经有了）。
- 其余功能**零依赖**，不需要 pyautogui / pynput / AutoHotkey。
